{
  "name": "b2b-tender-project",
  "version": "1.0.0",
  "description": "B2B Tender Management Platform",
  "scripts": {
    "start": "cd backend && npm start",
    "install": "cd backend && npm install",
    "build": "cd backend && npm install",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
